import { makeStyles } from "@mui/styles"

export const useStyles = makeStyles(() => ({
  layout: {
    margin: "0 auto",
    justifyContent: "space-between",
  },
  container: {
    marginLeft: 170,
  },
  containerPage: {
    padding: "0 56px",
  },
}))
