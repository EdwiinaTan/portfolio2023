import { makeStyles } from "@mui/styles"

export const useStyles = makeStyles(() => ({
  layout: {
    display: "flex",
    margin: "0 auto",
    justifyContent: "space-between",
  },
  container: {
    marginLeft: 170,
    width: "100%",
  },
  containerPage: {
    padding: "0 56px",
  },
}))
